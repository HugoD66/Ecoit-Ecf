{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
<div class="titre-formation"><h1>{{ title }}</h1></div>
<form class="connexion-form"  id="seconnecter">
    <div class="form-registry" id="form-css" action="{{ path('app_login') }}" method="post">
        <label  for="email">Email </label>
        <input type="email" class="form-control" id="email" name="_email" placeholder="ecoit@example.com"/>
    </div>
    <div class="form-group" id="form-css">
        <label for="password">Password</label>
        <input type="password" class="form-control" id="password" placeholder="Entrez votre mot de passe ici" name="_password">
    </div>
    <button class="button" type="submit"><span></span>Connexion</button>
    <div class="premiere-visite">
        <button class="button" type="button"><span></span><a href="{{ path('register') }}">Première visite ?</a></button>
    </div>
</form>

{% endblock %}